<!DOCTYPE html>
<html lang="en">
  <% include partials/head %>
  <body>
    <div class="section">
      <div class="container">
        <p class="title">Sign up</p>
        <form method="post" id="sign-up-form">
          <div class="field">
            <label class="label">First Name*</label>
            <div class="control">
              <input class="input" type="text" name="userfirstname" required>
            </div>
          </div>
          <div class="field">
            <label class="label">Last Name*</label>
            <div class="control">
              <input class="input" type="text" name="userlastname" required>
            </div>
          </div>
          <div class="field">
            <label class="label">Email*
              <% if (typeof emailInUse !== 'undefined' && emailInUse) { %>
              <span class="has-text-danger has-text-weight-semibold">already registered</span>
              <% } %>
              <% if (typeof invalidEmail !== 'undefined' && invalidEmail) { %>
              <span class="has-text-danger has-text-weight-semibold">invalid email</span>
              <% } %>
            </label>
            <div class="control">
              <input class="input" type="email" name="useremail" required>
            </div>
          </div>
          <div class="field">
            <label class="label">Password*
              <% if (typeof passwordCheckLength !== 'undefined' && passwordCheckLength) { %>
              <span class="has-text-danger has-text-weight-semibold">check length</span>
              <% } %>
            </label>
            <div class="control">
              <input class="input" type="password" name="userpassword" required>
            </div>
          </div>
          <div class="control">
            <button class="button is-primary" type="submit">Sign up</button>
          </div>
        </form>
      </div>
    </div>
  </body>
</html>
