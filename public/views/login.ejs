
<!DOCTYPE html>
<html lang="en">
  <% include partials/head %>
  <body>
    <% include partials/topbar %>
    <div class="section">
      <div class="container">
        <p class="title">Login</p>
        <form method="post" id="login-form">
          <div class="field">
            <label class="label">Email
              <% if (typeof userNotFound !== 'undefined' && userNotFound) { %>
              <span class="has-text-danger has-text-weight-semibold">not found</span>
              <% } %>
            </label>
            <div class="control">
              <input class="input" type="text" name="useremail" required>
            </div>
          </div>
          <div class="field">
            <label class="label">Password
              <% if (typeof wrongPassword !== 'undefined' && wrongPassword) { %>
              <span class="has-text-danger has-text-weight-semibold">incorrect</span>
              <% } %>
            </label>
            <div class="control">
              <input class="input" type="password" name="userpassword" required>
            </div>
          </div>
          <div class="control">
            <button class="button is-primary" type="submit">Login</button>
          </div>
        </form>
      </div>
    </div>
  </body>
</html>
